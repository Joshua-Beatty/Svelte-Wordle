{
  "name": "svelte-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "start": "npm run build && http-server public/"
  },
  "devDependencies": {
    "esbuild": "^0.14.47",
    "esbuild-svelte": "^0.7.1",
    "http-server": "^14.1.1",
    "svelte": "^3.0.0"
  },
  "dependencies": {
    "sirv-cli": "^2.0.0"
  }
}
